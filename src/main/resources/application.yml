spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shopnest?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate           # không tạo lại bảng
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379

  # Email Configuration (Gmail SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: hbst2308@gmail.com
    password: aums voxb jayo gdze
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

jwt:
  secret: 6Ly4d0F1eXNhbXBsZVNlY3JldEtleUZvclRlc3RpbmcxMjM0NTY=   # 256-bit
  expiration: 86400000   # 24h

# MoMo Payment Configuration
# Đăng ký tại: https://business.momo.vn/
momo:
  partner-code: ${MOMO_PARTNER_CODE:MOMO}
  access-key: ${MOMO_ACCESS_KEY:F8BBA842ECF85}
  secret-key: ${MOMO_SECRET_KEY:K951B6PE1waDMi640xX08PD3vg6EkVlz}
  endpoint: ${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api}
  ipn-url: ${MOMO_IPN_URL:http://localhost:8080/api/payment/momo/ipn}
  redirect-url: ${MOMO_REDIRECT_URL:http://localhost:8080/payment/momo/return}
  request-type: captureWallet